apiVersion: apps/v1
kind: Deployment
metadata:
  name: fdx-webapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fdx-webapp
  template:
    metadata:
      labels:
        app: fdx-webapp
    spec:
      containers:
      - name: fdx-webapp
        image: thippeshanaik/fdx-webapp:6
        ports:
        - containerPort: 8080

